using NSubstitute;
using NUnit.Framework;
using UnityEngine;

public class TestCalculatePoints
{
    [Test]
    public void TestGetEventlySpacedPoints()
    {
        // ARRANGE
        // Spline spline = new GameObject("test", typeof(Spline)).GetComponent<Spline>();
        // spline.LengthPoints.Returns(4);
        // float spacing = 0.3f;
        
        // ACT
        // Vector3[] points = CalculatePoints.GetEventlySpacedPoints(spline, spacing);

        // ASSERT
    }
    
}
